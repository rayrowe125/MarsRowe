<%= render 'devise/registrations/new', locals: { user: @user } %>

<div class="row" style="margin-top: 50px;">
  <div class="small-4 large-12 small-centered columns">
    <% @shirts.each do |shirt| %>
    <%= image_tag shirt.image.url(:thumb), style: 'margin-bottom: 5px;' %>
    <%#= link_to "Delete", shirt, method: :delete %>
    <% end %>
  </div>
</div>