
<div class="small-12 large-5 large-offset-1 columns">
  <%= image_tag 'buttonup.jpg' %>
</div>

<%= render 'devise/registrations/new', locals: { user: @user } %>

<div class="row" style="margin-top: 50px;">
  <div class="small-4 large-12 small-centered columns">
    <div style="text-align: center; margin: 0 auto;">
    <% @shirts.each do |shirt| %>
      <%= image_tag shirt.image.url(:thumb) %>
    <% end %>
  </div>
  </div>
</div>